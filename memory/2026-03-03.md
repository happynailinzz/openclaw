
- [social-intel-sop 02:00 UTC] 完成一次全流程（降级执行）：主通道可用 Reddit/News/RSS；X/小红书/YouTube 抓取受限。产出：memory/social-intel/latest_brief.txt, latest_structured.json

## 2026-03-03 09:00-09:37 UTC

### qmd 本地知识库搜索引擎 — 安装与配置完成
- 用户发来 https://github.com/tobi/qmd 项目，要求学习并评估对工作流的帮助
- qmd v1.0.7 已全局安装（npm install -g @tobilu/qmd），原来已预装，直接可用
- 三个 collection 已建立并配置 context：
  - `obsidian` → `/opt/openclaw/knowledge/secendME`（1375篇 Obsidian 笔记）
  - `articles` → `/root/.openclaw/workspace/articles`（22篇公众号文章）
  - `memory` → `/root/.openclaw/workspace/memory`（22篇记忆日志）
- BM25 关键词搜索已验证可用（测试查询"源网荷储 数字化"命中94%）
- 向量 embedding 后台运行中（1400+ 文档，需数分钟），完成后语义搜索与混合检索可用
- **工作流变更**：公众号写作时优先用 `qmd search` 查 obsidian 知识库，再决定是否外搜

### 社交情报简报 10:00 UTC 已发送
- 核心：OpenAI与美国国防部签协议、英国军采13亿、中东冲突升级
- 政策→交付风险映射已推送给用户

### OpenClaw 升级提醒
- 当前版本落后于 2026.3.1，已提示用户；用户未确认是否立即升级

## 2026-03-03 10:30-11:21 UTC

### 系统安全与稳定性加固（全部完成）

#### Swap 配置
- 服务器原本无 Swap，OOM 风险高
- 创建并启用 2G Swap：`fallocate -l 2G /swapfile` → `/usr/sbin/mkswap` → `/usr/sbin/swapon`
- 已写入 `/etc/fstab` 持久化，重启后自动生效
- 内存从 62% 水位降至 41%

#### 图片代理（8317）收口
- 原来 `0.0.0.0:8317` 对外暴露，任何人可调用消耗 API 配额
- 重新启动 docker 容器，绑定改为 `127.0.0.1:8317`
- 外网访问已被拒绝，本地 publish.sh 不受影响
- 容器名：`cli-proxy-api-plus`，镜像：`eceasy/cli-proxy-api-plus:latest`

#### Chrome 统一（Playwright 删除，节省 622MB 磁盘）
- 发现系统有两套 Chrome：Playwright（`~/.cache/ms-playwright/`，622MB）和 Puppeteer（`~/.cache/puppeteer/`，619MB）
- 两个 skill 都是 CDP 协议，实际只需一个 Chrome 二进制
- 删除 Playwright 缓存，全部统一到 Puppeteer Chrome
- 路径：`/root/.cache/puppeteer/chrome/linux-145.0.7632.77/chrome-linux64/chrome`
- 在 `.baoyu-skills/.env` 写入5个环境变量覆盖所有 skill：
  - `URL_CHROME_PATH`（baoyu-url-to-markdown）
  - `WECHAT_BROWSER_CHROME_PATH`（baoyu-post-to-wechat）
  - `X_BROWSER_CHROME_PATH`（baoyu-post-to-x）
  - `X_CHROME_PATH`（baoyu-danger-x-to-markdown）
  - `GEMINI_WEB_CHROME_PATH`（baoyu-danger-gemini-web）

#### 内存清理成果
- 删除 qmd embedding 模型（314MB，CPU 跑不起来的向量模型）
- 删除 node-gyp 构建缓存（65MB）
- 清理残留 Chrome 僵尸进程（释放约 1.6G 内存）
- 总计释放内存 ~1.6G，磁盘 ~1G

### 三系统评估（11:00 UTC）

#### 记忆系统评分：6/10
- 热记忆（日志文件）正常写入
- memory-retrieval-router.py 和 memory-session-hook.py 写好但未被自动调用（空转）
- MEMORY.md 只有31行，偏稀疏，需要本次工作后更新
- qmd 知识库接通但尚未经过实战写作验证

#### 进化系统评分：5/10
- agent-evolution-rules.md（7条规则）和 capability-tree.md（v0.1，13节点）设计完整
- EvoMap：credit=0，topTasks 始终为空，capsule 有数据（GDI 70.9）但无可接任务
- capability-tree.md 未随实际工作更新（今天的能力沉淀未入树）
- 无自动闭环机制，依赖心跳手动触发

#### 抗风险系统评分：7/10
- ✅ Swap、图片代理收口、密钥管理、Git 备份 cron、RESTORE.md 均已完成
- ❌ 无服务存活监控（代理挂了不知道）
- ❌ 宝塔面板（11840）和 VNC（8444）仍对外暴露
- ❌ OpenClaw 版本落后（2026.3.2 已发布）

### 待办（用户确认中）
1. capability-tree 更新（今日能力入树）
2. 心跳加入服务存活检查（图片代理 + gateway）
3. OpenClaw 升级到 2026.3.2

### GitHub 提交记录
- `62abbf6`：统一两个 skill 使用同一 Puppeteer Chrome；删除 Playwright（622MB）
- `a6c1367`：补全所有 skill 统一 Chrome 路径配置（post-to-x/danger系列）
